{ 
    "type": "categorical_regression",
    "arch_desc": {
        "component_channels": COMPCH,
        "out_channels": OUTCH,
        "grid": GRID,
        "step": STEP,
        "max_value": MAXVALUE,
        "train_step": TRAINST
    }
}

